{
  "name": "Ethara AI - Team Task Manager",
  "description": "Full-stack team collaboration and task management application",
  "builder": "nixpacks",
  "buildCommand": "cd frontend && npm install && npm run build && cd ../backend && npm install",
  "startCommand": "cd backend && npm start",
  "env": {
    "NODE_ENV": {
      "description": "Node environment (production)",
      "value": "production"
    },
    "PORT": {
      "description": "Server port",
      "value": "3000"
    },
    "MONGODB_URI": {
      "description": "MongoDB Atlas connection string. Example: mongodb+srv://username:password@cluster.mongodb.net/dbname",
      "required": true
    },
    "JWT_SECRET": {
      "description": "JWT secret key (min 32 chars). Generate: openssl rand -base64 32",
      "required": true
    },
    "CORS_ORIGIN": {
      "description": "CORS origin (your Railway app URL). Will be set automatically by Railway",
      "required": false,
      "value": "*"
    }
  },
  "volumes": [],
  "services": [
    {
      "type": "web",
      "name": "api",
      "port": 3000
    }
  ],
  "deploy": {
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}
